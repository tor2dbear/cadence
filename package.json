{
  "name": "cadence",
  "version": "0.9.5",
  "private": true,
  "description": "A motion system designer. Static site — this manifest exists only for the Playwright smoke tests, which never ship to the deployed site.",
  "scripts": {
    "test": "node tests/run.mjs"
  },
  "devDependencies": {
    "playwright": "1.56.1"
  }
}
